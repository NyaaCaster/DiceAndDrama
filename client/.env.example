# NyaaChat-MCP streamable-HTTP endpoint. Used by nginx envsubst at container
# start to inject the Bearer token so the API key never reaches the frontend.
MCP_HOST=http://h.hony-wen.com
MCP_PORT=3094
MCP_API_KEY=
