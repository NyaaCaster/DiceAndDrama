# Comma-separated list of origins allowed to call this service. Empty means
# CORS is disabled entirely (same-origin only via the client's nginx reverse
# proxy at /api/cloudsave/*). Add a host here only if a different game's
# frontend needs to call this service cross-origin.
ALLOWED_ORIGINS=

# Set to "production" so error responses don't leak stack traces.
NODE_ENV=production
